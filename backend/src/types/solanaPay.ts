export interface paymentType {
    
}

export interface GenerateUrlType {
    equipmentId: string, 
    amount: number, 
    recipient: string,
    label: string
    memo: string,
    image: string
}
